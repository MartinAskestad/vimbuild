name: Build vim

on:
  workflow_dispatch:

jobs:
  setup:
    name: Build vim
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: vim/vim
      - uses: ilammy/msvc-dev-cmd@1.13.0
      - run: |
          nmake -f Make_mvc.mak
        working-directory: src
      - run: dir /s *.exe
        shell: cmd
